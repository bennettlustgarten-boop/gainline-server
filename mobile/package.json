{
  "name": "gainline-ios",
  "version": "0.1.0",
  "private": true,
  "description": "Native iOS shell for Gainline — wraps the live site (gainlineapp.online) in a Capacitor app for App Store distribution.",
  "scripts": {
    "sync": "cap sync ios",
    "open:ios": "cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^7.0.0",
    "@capacitor/ios": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
