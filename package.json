{
  "name": "gainline-server",
  "version": "0.1.0",
  "description": "Backend for Gainline — coach onboarding, client payments, and platform fees via Stripe Connect.",
  "main": "server.js",
  "type": "commonjs",
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-rate-limit": "^8.7.0",
    "express-session": "^1.18.0",
    "helmet": "^8.3.0",
    "multer": "^2.0.0",
    "stripe": "^16.8.0"
  }
}
